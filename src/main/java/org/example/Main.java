/*
package org.example;


public class Main {
    public static void main(String[] args) {
        try {
            // Load the JSON file
            VerifiedPushConfigUtil.loadConfig("path/to/verified_push_template.json");

            // Fetch and print the full configuration
            VerifiedPushConfig config = VerifiedPushConfigUtil.getConfig();
            System.out.println("Config Version: " + config.getVersion());

            // Fetch and print a specific factor by ID
            VerifiedPushConfig.Factor factor = VerifiedPushConfigUtil.getFactorById(1);
            System.out.println("Factor Name: " + factor.getFactor_name());
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
*/
